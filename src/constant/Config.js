const apiVersion = "/api/v1";
// const apiURL = "https://www.okex.com";
const apiURL = "";

export const Config = {
  status: "staging",
  staging: {
    apiURL,
    apiVersion,
  },
  production: {
    apiURL,
    apiVersion,
  },
};
